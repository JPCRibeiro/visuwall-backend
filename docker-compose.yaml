services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: visuwall
      POSTGRES_USER: visuwall
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: